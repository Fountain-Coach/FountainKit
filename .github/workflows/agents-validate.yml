name: Agents Validate

on:
  push:
  pull_request:

jobs:
  validate-agents:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Validate agent descriptors (fountain.coach enforced)
        run: bash Scripts/ci/validate-agents.sh

