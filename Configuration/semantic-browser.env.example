# Semantic Browser server env example
# Copy to your shell or export variables before running semantic-browser-server.

# Prefer a remote Chrome DevTools Protocol browser (WebSocket URL)
# SB_CDP_URL=ws://127.0.0.1:9222/devtools/browser/<uuid>

# OR prefer a local browser CLI (uncomment and adjust path/flags)
# SB_BROWSER_CLI=/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome
# SB_BROWSER_ARGS=--headless=new --disable-gpu --disable-dev-shm-usage --remote-debugging-port=9222

# Bind port (defaults to 8007)
# SEMANTIC_BROWSER_PORT=8007
