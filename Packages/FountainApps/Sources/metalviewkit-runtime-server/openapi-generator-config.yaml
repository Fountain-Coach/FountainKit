generate:
  - types
  - server
filter:
  # Start narrow so the server compiles; expand as handlers land
  paths:
    - /health
    - /v1/midi/events
    - /v1/midi/endpoints
    - /v1/midi/vendor
accessModifier: internal
additionalImports: []
